# Build artifacts and model checkpoints (regenerated locally)
local_slm
*.bin
local_slm.bin

# Sub-agent scratch files
/tmp/slm_agent_snippet*

# Fetched training corpus (large, regenerated by fetch_corpus.sh)
corpus/
